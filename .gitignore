# secrets
.env
.env.*
!.env.example

# dependencies and runtime
node_modules/
logs/
*.log

# regenerated by scripts (npm run qr)
assets/qr/

# designers' raw originals: up to 1 GB each, and GitHub refuses files over 100 MB.
# The WhatsApp-ready versions live in assets/documents and assets/price-list.
source-material/

# superseded documents kept for reference
assets/documents/_replaced/

# guest data exports (real phone numbers)
db/*.backup.json

# tunnel binaries (55 MB): download cloudflared-windows-amd64.exe from
# github.com/cloudflare/cloudflared/releases into bin/cloudflared.exe
bin/
